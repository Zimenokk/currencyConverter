import React from 'react';
import {Stack, TextField} from "@mui/material";

const MuiInput = () => {
    return (
        <TextField
            // fullWidth
            size={'small'}
            variant={'standard'}
        />
    );
};

export default MuiInput;
